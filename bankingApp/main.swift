
//logIn1()
//chooseOperation()

var currentUser = Account.init(name: getName(), amount: 0)

func searchForAcc(name: String) {
    for acc in accountsArrey {
        if name == accountsArrey
    }
    
    if accountsArrey.contains(where: {$0.name == getName()}) {
        // вытащить даблы из массива и положить в currentUser
      let currentUser = Account.init(name: getName(), amount: 0)
    } else {
        
        
    }
}


